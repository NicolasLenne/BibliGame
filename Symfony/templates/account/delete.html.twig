{% extends 'member.html.twig' %}

{% block title %}Supprimer mon compte{% endblock %}

{% block body %}
    <h3 class="title is-3 has-text-centered">Supprimer mon compte</h3>
    <div class="is-flex is-flex-direction-column px-2 is-flex-mobile">
        <div>
        <p>Afin de pouvoir confirmer la suppression de votre compte, veuillez confirmer votre choix à l'aide de votre mot de passe :</p>
        
        <form action="{{ path('app_account_delete') }}" method="post">
        <div class="field">
            <label class="label"></label>
            <div class="control">
                <input class="input" type="password" name="password" placeholder="Mot de passe">
            </div>
        </div>
        <input type="hidden" name="token" value="{{ csrf_token('delete') }}"/>
        <button class="button is-danger is-rounded mt-4">Supprimer</button>
        </form>
        
        </div>
    </div>
{% endblock %}